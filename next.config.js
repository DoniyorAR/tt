/** @type {import('next').NextConfig} */
const nextConfig = {
  // async redirects() {
  //   return [{ source: '/', destination: '/overall', permanent: false }];
  // },
};
module.exports = nextConfig;
